# MVP Demo 需求拆解

> 版本 v0.4 ｜ 2026-07-21 ｜ **独立 demo,不依赖也不改动 REQUIREMENTS/ADR/design 分析**
> 目标平台:**麒麟操作系统真机**良好运行;登录/权限全部 **mock**。
> **文档优先级**:本文是 MVP Demo 的唯一执行与验收依据;生产化 REQUIREMENTS、CAS、国密等文档不阻塞 Demo。

## 1. 目标可见结果(验收即演示)
1. **Mock 登录成功后进入工作台**。
2. **至少两个 Mock 角色显示不同菜单**(切换角色 → 菜单随权限变化)。
3. **内嵌一个指定 Web 页面**,支持**加载、刷新、错误提示**。
4. **从白名单拉起一个指定麒麟应用**;**未安装时提示明确**。
5. **K 线示例**:蜡烛图、缩放、平移、十字线,数据为 **Mock 行情**。
6. **在指定麒麟真机上启动、切换、操作无明显卡顿**。

## 2. 安全红线(demo 也必须保留,不得为省事去掉)
- **S1** QProcess **只能启动清单中的固定程序**,**禁止拼接 Shell 命令**(参数固定/白名单,不经 shell)。
- **S2** WebView **仅允许指定 URL/Origin**(白名单,越界拦截)。
- **S3** Mock 登录**不得使用真实银行账号或凭证**(纯假数据)。
- **S4** K 线**只用 Mock 数据**(不连真实行情)。
- **S5** 无实际需求时,**不实现 JSBridge 的文件/设备/凭证能力**(最小暴露,能不做就不做)。

## 3. Demo 输入与前置决策

| 项 | 状态 | 要求 / 默认值 |
|---|---|---|
| 验收真机 | **待确认** | 记录麒麟 V10 子版本、CPU 架构/型号、Qt/QtWebEngine 版本 |
| 开发语言 | **已定** | C++/QML + CMake |
| Web 演示页 | **待确认,不阻塞外壳开工** | 默认用**本地打包测试页**,不依赖公网;验收前替换为指定内网/HTTPS URL |
| 原生拉起目标 | **待确认,不阻塞外壳开工** | 在验收机上从计算器/文件管理器中选一个确定程序和绝对路径 |
| K 线图表库 | **待确认,不阻塞外壳开工** | 从 klinecharts / ECharts 选定一个,锁定版本和许可证,资源全部本地化 |
| K 线 Mock 数据 | **待确认** | 固定数据集与数据量,不连接真实行情 |

> Web 页、原生应用和图表库不阻塞 T1/T2 外壳开发;验收真机环境须在 T1 麒麟冒烟验收前锁定。所有内容项必须在各自功能任务验收前锁定。

## 4. 模块拆解

> 本节 `M1–M5` 表示 Demo 功能模块,不等同于生产化 REQUIREMENTS 中的里程碑。

| 模块 | 内容 | 对应结果 |
|---|---|---|
| **M1 门户外壳** | Qt/QML 原生窗口:顶栏 + 菜单/工作台 + 标签容器;麒麟真机正常启动/渲染/交互 | 1、6 |
| **M2 登录+权限(mock)** | mock 登录页(假账号)→ mock 接口返回角色与菜单权限 → 工作台按角色渲染菜单;**≥2 角色** | 1、2 |
| **M3 内嵌 web** | 标签内 QtWebEngine 加载**指定 URL**;工具条含**刷新**;加载失败显示**错误提示**;仅白名单 Origin(S2) | 3 |
| **M4 拉起原生应用** | 从**清单白名单**用 QProcess 拉起指定麒麟应用;**固定程序+固定参数,不过 shell**(S1);未安装 → **明确提示** | 4 |
| **M5 K 线示例** | 平台内模块页:QtWebEngine 加载**本地打包的 web 图表页**(klinecharts/ECharts),蜡烛图 + 成交量 + **缩放/平移/十字线**,**mock 数据**(S4) | 5 |

## 5. 任务拆解与依赖

> 工作量按 1 名熟悉 Qt/QML 的开发者、麒麟环境可用估算;不含环境/设备申请等待时间。

| ID | 任务 | 子任务 | 产出 / 验收 | 依赖 | 估算 |
|---|---|---|---|---|---|
| **T0A** | 验收环境基线 | 记录验收机 OS/CPU/Qt/QtWebEngine;确认构建与运行通道 | 验收机信息已回填;可完成麒麟冒烟验收 | 无 | 0.25d |
| **T0B** | 演示内容基线 | 选定 Web URL、原生应用、图表库/版本/许可证、Mock 数据量 | 第 3 节内容项已回填;依赖资源可离线获取 | 无 | 0.25d |
| **T1** | Qt/CMake 工程骨架 | 创建单一可执行工程;引入 Qt Quick/Controls/WebEngine;建立 `qrc` 资源;基础日志 | Mac/开发环境可编译;麒麟真机可启动空窗口 | T0A 在麒麟冒烟验收前完成 | 0.5–1d |
| **T2** | M1 门户外壳 | 登录/工作台页面容器;顶栏、侧边菜单、标签容器;空、加载、错误状态 | 工作台布局稳定;打开/关闭/切换标签不崩溃 | T1 | 1–1.5d |
| **T3** | 清单与模块注册 | 定义/校验 Manifest;编译进 `qrc`;用户→角色→模块映射;模块类型分发 | 运行时不从用户可写路径读取 `exec`/URL;无效清单拒绝启动 | T1 | 0.5–1d |
| **T4** | M2 Mock 登录与权限 | 本地 Mock 服务(不搭 HTTP 后端);两个假用户/角色;登录成功/失败;退出;菜单过滤 | 用户 A/B 菜单不同;错误凭证有提示;无真实凭证 | T2、T3 | 0.5–1d |
| **T5** | M3 Web 内嵌 | WebEngine 标签;加载/刷新/错误页;限制主页面跳转、重定向和弹窗;处理白名单 | 指定页可用;越界导航被拦截;断网/无效 URL 有可恢复错误提示 | T2、T3、T0B 的 Web URL | 1–1.5d |
| **T6** | M4 原生应用拉起 | 校验固定路径/可执行;使用 `QProcess::start(program, args)`;启动失败处理 | 白名单应用能启动;缺失时明确提示;不经 Shell;无用户可控路径/参数 | T3、T0B 的目标应用 | 0.5d |
| **T7** | M5 K 线示例 | 图表库/Mock 数据本地化;蜡烛图、成交量、缩放/平移/十字线;嵌入平台标签 | 断网仍可完整演示;无真实行情;不实现特权 JSBridge;图表操作无卡死 | T2、T3、T0B 的图表库/数据量 | 1.5–2d |
| **T8** | 真机集成与演示验收 | 按第 9 节执行全部用例;记录启动耗时/内存(仅基线);整理构建运行说明、演示脚本和已知问题 | A01–A12 通过;产出验收记录 | T4–T7 | 1d |

**预估合计**:环境可用且无 QtWebEngine 版本阻塞时,1 人约 **7–10 个开发日**。设备申请、内外网摆渡、目标页适配不计入。

### 建议执行顺序

```text
T0A → T1 → T2 → T3 → ┌─ T4 Mock 登录
                           ├─ T5 Web 内嵌 ← T0B(Web URL)
                           ├─ T6 原生拉起 ← T0B(目标应用)
                           └─ T7 K 线 ← T0B(图表库/数据)
T4 + T5 + T6 + T7 → T8 真机验收
```

## 6. 技术决策
- **开发语言:C++/QML**(与产品一致,demo 可演进为 M0 骨架)。
- **K 线实现形态已定**:内嵌 web 图表页,复用 QtWebEngine;具体图表库在 T0B 从 klinecharts/ECharts 中选一个并锁版本。
- **Mock 实现**:应用内本地桩/本地 JSON,不搭建独立 HTTP Mock 服务。
- **K 线数据传递**:默认由本地图表页直接读取 Mock 数据,不为 Demo 开发通用 JSBridge。

### 仍待确认(小,不阻塞开工)
1. **拉起目标**:用哪个麒麟自带应用作示例(默认:计算器 `kylin-calculator` 或文件管理器)。
2. **内嵌页面**:指定哪个 URL(默认:先用本地打包占位页,后替换为指定内网/HTTPS 页)。
3. **图表库**:klinecharts 或 ECharts,及其锁定版本/许可证。

## 7. 清单驱动(贯穿 M2–M5,落地安全红线)
一份**编译进 `qrc`、运行时只读**的 `manifest.json`是 Demo 用户、角色、模块与白名单的唯一配置来源。不从用户可写目录覆盖或合并清单:
```jsonc
{
  "users": {
    "demoA": { "password": "demo-only", "role": "roleA" },
    "demoB": { "password": "demo-only", "role": "roleB" }
  },
  "roles": { "roleA": ["appWeb","appKline"], "roleB": ["appWeb","appNative"] },
  "modules": [
    { "id":"appWeb",    "type":"web",    "url":"https://<指定>",        "allowedOrigins":["https://<指定>"] },
    { "id":"appNative", "type":"native", "exec":"/usr/bin/<固定程序>", "args":[] },
    { "id":"appKline",  "type":"kline" }
  ]
}
```
- `type:web` → `allowedOrigins` 至少约束**顶层导航、重定向和新窗口**;若目标页依赖其他域的子资源,验收前明确资源 Origin 列表(S2)。
- `type:native` → 仅 `exec` 固定路径 + 固定 `args`,QProcess 不经 shell(S1)。
- 角色→模块映射驱动菜单(结果 2)。
- Manifest 解析失败、字段缺失、类型未知或重复 `id` 时**拒绝进入工作台**,显示配置错误。

## 8. 组件结构(建议)
```
demo/                 C++/QML 工程(CMake)
  shell/              门户外壳(窗口 + 菜单 + 标签容器)
  mock-api/           mock 登录/权限(本地桩,假数据 S3)
  web-kline/          本地 web 图表页(klinecharts/ECharts)+ mock 行情(S4)
  manifest.json       用户/角色/模块/白名单(编译进 qrc)
```

## 9. 验收用例

| ID | 用例 | 通过标准 |
|---|---|---|
| **A01** | Mock 用户 A 登录 | 成功进入工作台,只显示 roleA 授权模块 |
| **A02** | Mock 用户 B 登录 | 成功进入工作台,菜单与 A 明确不同 |
| **A03** | 错误 Mock 凭证 | 不进入工作台,显示明确但不泄露内部信息的错误 |
| **A04** | 退出登录 | 清理本地 Mock 会话,返回登录页 |
| **A05** | 打开/刷新 Web 页 | 指定页正常显示,刷新后可继续使用 |
| **A06** | Web 加载失败 | 显示错误原因摘要和重试入口,外壳不崩溃 |
| **A07** | Web 越界跳转/弹窗 | 未授权 Origin 不在容器内打开,显示已拦截提示 |
| **A08** | 拉起白名单麒麟应用 | 使用 QProcess 直接启动固定程序,不经 Shell |
| **A09** | 目标应用缺失/不可执行 | 门户显示明确错误,不尝试其他路径或程序 |
| **A10** | K 线基本展示 | 蜡烛图和成交量使用固定 Mock 数据完整渲染 |
| **A11** | K 线交互 | 缩放、平移、十字线可用;断网状态仍可演示 |
| **A12** | 麒麟真机冒烟 | 完整走通 A01–A11;连续打开/切换模块无崩溃、无长时间卡死 |

## 10. 交付物与完成定义(DoD)

- [ ] T0A/T0B 所有环境/依赖决策已回填,图表库版本和许可证已锁定。
- [ ] C++/QML 源码、CMake 构建配置、只读 Manifest、本地 Web/K 线资源齐全。
- [ ] 在指定麒麟真机上完成从构建/启动到演示的闭环。
- [ ] A01–A12 全部通过,验收机环境与结果有记录。
- [ ] 登录/权限/行情均为 Mock,无真实账号、凭证和业务数据。
- [ ] K 线不依赖公网/CDN;未实现无关特权 JSBridge。
- [ ] 提供简短的构建/运行说明、演示脚本、已知问题清单。
- [ ] 记录一次启动耗时与空载/K 线打开后内存,仅作后续基线,不声称生产 SLA。

## 11. 诚实边界
- 登录/权限/行情均 mock,**不证明**真 CAS/权限中心/行情集成。
- 以"良好运行 + 效果可演示 + 无明显卡顿"为准,**不做**性能量化/国密/打包结论(见 M-1)。
- 原生拉起为**启动器式**(独立窗口),非嵌入 Tab(嵌入受 Wayland 限制,见分析文档)。
- Demo 不实现真实 CAS、权限中心、国密、审计、Profile 隔离、SLO、自动更新或原生 IPC。
