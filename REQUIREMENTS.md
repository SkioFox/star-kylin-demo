# 银行麒麟超级应用平台 —— 需求分析与拆解

> 文档版本:v0.4 ｜ 日期:2026-07-21 ｜ 状态:待评审
> 技术栈基线:**Qt/QML (C++) + QtWebEngine** ｜ 目标架构:**ARM64(飞腾/鲲鹏)+ x86_64**
> 项目性质:**信创迁移 + 应用集成平台**(银行内网)
> 认证基线:**CAS 单点登录**(CAS Cookie 免登,不排除 Token) ｜ 权限:**对接行内权限中心** ｜ 系统:**麒麟 V10**
> v0.4 变更:回填 §7 阻塞项答案,确立 CAS/权限中心/V10 基线;明确原生集成"统一应用体验"诉求与 Wayland 约束的取舍

---

## 1. 背景与目标

### 1.1 一句话定义
在**麒麟操作系统**上构建一个**银行内网超级应用平台**,作为统一入口与容器,把行内各类 **B/S web 应用**集成进来,打通**行内企业认证与权限体系**,提供**子应用开发框架**与统一基础能力,交付**媲美原生**的性能与体验。

### 1.2 项目本质(基于银行背景修正)
这不是一个"通用超级 App",而是一次**信创国产化迁移**:随着行内 PC 从 Windows 迁往麒麟,需要一个统一平台承接现有一批 web 应用,并统一解决"登录一次、到处可用""权限集中管控""安全合规"这些横切问题。

因此平台的核心价值是三件事:
1. **统一门户与容器** —— 一个入口聚合 N 个 web 子应用,体验一致。
2. **统一认证与权限** —— 对接行内企业认证(4A/统一身份),单点登录,登录态与权限向子应用透传。
3. **安全合规底座** —— 满足银行等保、国密、审计要求,子应用"零成本"继承合规能力。

> 相比 v0.1:集成形态**收敛为 B/S web 内嵌**,容器复杂度大幅下降(无需进程托管/窗口聚合/Wine 兼容层);资金领域 K 线等组件降级为**平台之上的一类业务子应用示例**,不再是底座必选项。

### 1.3 分层视角
| 层 | 性质 | 内容 |
|---|---|---|
| 平台底座 | 通用可复用 | 认证、权限、web 容器、通信、缓存、能力桥、安全合规 |
| 子应用层 | 业务方各自开发 | 行内各 web 系统 + 新建子应用(含资金类 K 线组件等) |

---

## 2. 技术栈决策记录(ADR)

| 决策 | 结论 | 理由 |
|---|---|---|
| 底座框架 | **Qt/QML (C++)** | 麒麟官方 SDK 即 Qt 生态;原生性能最强;唯一能兑现"媲美原生"的选项 |
| web 容器 | **QtWebEngine(Chromium)** | 承载 B/S 子应用;统一会话/JSBridge/安全策略;主集成形态 |
| 目标架构 | **ARM64 + x86_64 双架构** | 行内 PC 含国产 ARM 芯片(飞腾/鲲鹏),ARM64 为主战场 |
| 打包 | **.deb + 麒麟软件商店/行内分发** | 麒麟基于 Debian 系;银行通常走内网软件分发/准入 |
| 密码算法 | **国密 SM2/SM3/SM4 优先** | 银行合规硬要求,通信/存储/签名全链路支持 |
| 单点登录 | **CAS 协议** | 行内认证基线;门户完成 CAS 登录后,子应用凭 CAS 会话免登 |
| 免登透传 | **共享 CAS Cookie 为主,Token 注入为辅** | 门户与子应用同域/信任域下共享 CAS 票据;不排除后端 Token 换取 |
| 权限来源 | **对接行内权限中心** | RBAC 数据由权限中心提供,门户只做加载与本地鉴权,不自维护权限主数据 |
| 系统基线 | **麒麟 V10** | 目标操作系统统一为 V10;具体子版本与自带 Qt/合成器待真机核定 |

> 被否方案:Electron(内存/启动/帧率达不到原生目标,银行对资源占用敏感);Tauri(麒麟系统 WebView 碎片化)。

---

## 3. 需求清单

### 3.1 麒麟 OS 适配(R1)
- **R1.1** 支持 x86_64 与 ARM64(飞腾/鲲鹏)双架构编译与运行。
- **R1.2** 交付 `.deb` 安装包,符合麒麟准入规范,支持行内内网分发/静默安装。
- **R1.3** 系统集成:窗口管理、系统托盘、桌面通知、DPI 高分屏缩放、深浅色主题跟随系统。
- **R1.4** 兼容麒麟 **V10**(桌面版)主流版本,明确最低系统子版本与自带 Qt 版本基线。

### 3.2 应用底座(R2)
- **R2.1 企业认证**:对接行内 **CAS 单点登录**;门户完成 CAS 登录后建立全局会话;登录方式含账密、CA 证书、可选扫码;CAS 票据/会话生命周期与续期;强制注销与全局会话失效(CAS 单点登出)。
- **R2.2 权限管控**:**对接行内权限中心**加载 RBAC(角色-资源-操作),门户不自维护权限主数据;门户菜单级 + 子应用功能级鉴权;权限与用户身份向 web 子应用**安全透传**。
- **R2.3 web 子应用容器**:子应用注册/加载/隔离/卸载;多标签或多窗口导航;生命周期与状态管理;进程/会话隔离策略。
- **R2.4 能力桥 JSBridge**:web ↔ 原生双向通信;子应用可调用底座能力(SSO 态、通信、缓存、通知、打印、文件、设备等),受权限与域名白名单严格约束。
- **R2.5 子应用开发框架**:提供子应用接入规范、JS SDK、脚手架/示例、联调工具,降低业务方接入成本。

### 3.3 基础能力 SDK(R3)
- **R3.1 通信**:HTTP 客户端(拦截器/重试/统一鉴权头/国密 TLS)、WebSocket(自动重连/心跳/订阅)、SSE。
- **R3.2 缓存**:内存缓存(LRU)+ 磁盘持久化(SQLite/KV,**敏感数据加密落盘或不落盘**)+ TTL/失效策略。
- **R3.3 WebView 能力**:QtWebEngine 承载;统一 Cookie/会话共享;JSBridge 注入;域名白名单与内容安全策略(CSP)。

### 3.4 安全与合规(R4)—— 银行硬门槛
- **R4.1 国密支持**:通信 TLS、数据存储、签名验签优先采用 SM2/SM3/SM4。
- **R4.2 等保合规**:满足行内等保定级要求(登录失败锁定、密码策略、超时锁屏、最小权限等)。
- **R4.3 审计日志**:关键操作(登录、鉴权、子应用访问、敏感能力调用)可审计、可追溯,对接行内日志/审计平台。
- **R4.4 数据安全**:敏感数据传输加密、存储加密或不落地;防截屏/水印(按行内要求可选);剪贴板/文件外发管控。
- **R4.5 应用完整性**:安装包签名校验、防篡改、防逆向(按行内标准)。

### 3.5 性能要求(R5,横切约束)
- **R5.1** 冷启动时间目标(待基线测定,建议 < 2s)。
- **R5.2** 常驻内存占用可控(明显优于 Electron 同类)。
- **R5.3** 子应用切换流畅、无白屏卡顿;长连接断线自愈稳定。

---

## 4. 模块架构拆解

```
┌─────────────────────────────────────────────────────────┐
│                     子应用层(业务方)                     │
│   行内已有 web 系统   │   新建子应用(资金/K线 等)        │
├─────────────────────────────────────────────────────────┤
│                     平台底座层                            │
│  认证中心 │ 权限引擎 │ web 容器 │ JSBridge │ 子应用框架   │
├─────────────────────────────────────────────────────────┤
│                 安全合规层(横切)                        │
│  国密 │ 审计日志 │ 数据加密 │ 完整性校验 │ 准入          │
├─────────────────────────────────────────────────────────┤
│                   基础能力 SDK 层                         │
│  通信(HTTP/WS/SSE) │ 缓存(内存/磁盘) │ WebEngine 封装 │
├─────────────────────────────────────────────────────────┤
│                  麒麟 OS 适配层                           │
│  双架构编译 │ 打包签名分发 │ 系统集成(托盘/通知/DPI/主题)│
└─────────────────────────────────────────────────────────┘
```

模块清单:
1. `platform/` 麒麟适配(构建、打包、系统集成)
2. `core/auth` 认证中心(SSO / 4A 对接)
3. `core/rbac` 权限引擎
4. `core/container` web 子应用容器 + JSBridge
5. `core/sdk-js` 子应用开发框架与 JS SDK
6. `sdk/net` 通信(HTTP/WS/SSE,国密 TLS)
7. `sdk/cache` 缓存(加密落盘)
8. `security/` 国密、审计、数据安全(横切)
9. `perf/` 性能基线与埋点(横切)

---

## 5. 里程碑规划

| 里程碑 | 目标 | 验收要点 |
|---|---|---|
| **M0 骨架** | Qt 工程 + 双架构编译打包,可启动门户空壳 | x86 与 ARM64 均能出 `.deb` 并启动 |
| **M1 平台命脉** | SSO 登录 + 内嵌一个行内 web 应用 + JSBridge 打通 | 登录一次进门户,子应用免登直用 |
| **M2 容器与权限** | 多子应用注册/多标签 + RBAC 菜单与功能鉴权 | 权限正确透传子应用 |
| **M3 基础能力+安全** | HTTP/WS/SSE + 缓存 + 国密 + 审计日志 | 通信国密可用,关键操作可审计 |
| **M4 开发框架+性能** | 子应用 JS SDK + 脚手架 + 性能打磨 | 业务方可独立接入;满足 R5 |

---

## 6. 主要风险与技术难点

| 风险 | 说明 | 应对 |
|---|---|---|
| SSO 免登透传 | 门户登录态如何安全传给各 web 子应用(Cookie/Token 注入/票据换取) | 早期与行内认证系统联调,定统一免登方案 |
| 国密适配 | Qt/Chromium 默认不支持国密 TLS,需集成国密库/改造 | 评估 GmSSL/铜锁等,或反向代理侧终止国密 |
| ARM64 依赖兼容 | 第三方库在飞腾/鲲鹏上可能无预编译包 | 早期在真机/交叉编译验证依赖矩阵 |
| 子应用安全边界 | JSBridge 暴露原生能力有越权风险 | 白名单 + 权限校验 + 能力最小授权 + 审计 |
| 麒麟/Qt 版本碎片 | 不同麒麟版本 Qt/WebEngine 差异 | 明确基线,自带运行时依赖 |
| 合规评审周期 | 银行安全评审可能阻塞上线 | 安全需求前置,M3 即接入审计与国密 |

---

## 7. 决策与待确认事项

### 7.1 已确认基线(2026-07-21)
| # | 事项 | 结论 |
|---|---|---|
| 1 | 企业认证 | **CAS 单点登录**(可能叠加 CA 证书登录,待核实) |
| 2 | 子应用免登 | **共享 CAS Cookie 为主**,不排除 Token 注入 |
| 3 | 权限系统 | **对接行内权限中心**,权限模型粒度与权限中心保持一致,门户不自维护 |
| 4 | 系统 | 麒麟 **V10** |
| 5 | PC 原生集成 | 面向**已适配麒麟的原生应用**;体验上"启动器拉起"与"Tab 内嵌"皆可,**倾向"整体是一个应用"的统一体验**(受 §8.2 Wayland 约束,见 §8.4) |
| 6 | 版本管理 | git 已初始化 |

### 7.2 仍待确认(阻塞项)
1. **CAS 细节**:CAS 版本/协议(CAS 2.0/3.0、是否 REST)?门户与子应用是否同信任域(决定 Cookie 能否直接共享)?是否强制 CA 证书登录?
2. **权限中心接口**:权限中心的对接协议与数据模型(接口/SDK、角色-资源-操作的具体粒度)?
3. **国密要求范围**:哪些链路强制国密(通信/存储/签名)?是否有指定国密库或密码机?——**当前不确定,需向行内安全部门确认**,直接影响 M3。
4. **Qt 版本基线**:V10 具体子版本自带的 Qt/QtWebEngine 版本,以及默认合成器是 **X11 还是 Wayland**(决定原生 Tab 内嵌是否可行)。
5. **首批接入子应用清单**:先集成哪几个 web 系统?各自现有登录/权限现状。
6. **合规基线**:等保定级、审计平台对接方式、数据落地/防截屏等具体要求。

---

## 8. 平台扩展性:PC 端应用集成可行性

平台初期以 web 集成为主,但需评估将来承接 **PC 端应用**的可行性。在麒麟(Linux)上,"PC 应用"分三类,可行性天差地别。

### 8.1 三类 PC 应用,三种命运

**① 已适配麒麟的原生应用(C/S,已完成信创迁移)—— 可行,推荐扩展方向**

| 集成深度 | 做法 | 可行性 | 代价与限制 |
|---|---|---|---|
| **拉起式(推荐)** | 门户作启动器,`QProcess` 拉起独立应用,统一入口/托盘/任务管理 | 高 | 低;子应用为独立窗口,不在容器 Tab 内 |
| **窗口嵌入(XEmbed)** | X11 reparent,把外部窗口塞进 Tab | 中 | 高;**Wayland 下基本失效**,麒麟新版默认 Wayland,路径在收窄 |
| **同进程加载** | 外部应用改造为 Qt 插件/动态库,`QPluginLoader` 加载 | 中低 | 极高,需源码改造,近似重写接入层 |

**② 存量 Windows C/S 应用 —— 不建议"集成",应走迁移**
- Wine/兼容层:内网稳定性、合规、维护性均为灾难,不用于生产。
- 云化/VDI 推流:本质是远程桌面,非真正集成,成本与体验另议。
- 重构为 B/S 或麒麟原生:最干净,但属于**迁移**而非集成。
- 结论:存量 Windows C/S 一律走迁移路线,不纳入平台"集成"。

**③ 新建原生子应用 —— 完全可行,理想形态**
直接基于子应用框架/SDK 开发,天然在容器内、天然继承认证与权限。

### 8.2 关键技术卡点:Wayland
麒麟较新版本默认 **Wayland** 合成器。"把其他应用窗口嵌入自身窗口"(XEmbed / window reparenting)在 Wayland 安全模型下**基本不可用**。因此"原生应用无缝嵌在 Tab 内"的同花顺式体验在麒麟上很可能无法实现,只能退化为"**启动器 + 独立窗口**"。**此风险须早在真机验证,勿留到后期。**

### 8.3 架构应对:把容器抽象为"集成协议"
不在当前实现,但**将容器抽象为多种集成类型**,web 仅为其一,认证/权限/审计/启动入口在"子应用注册表"这层统一收口:

```
子应用注册表(统一元数据:图标/权限/认证/生命周期)
   ├─ type: web        → QtWebEngine 内嵌        (M1 落地)
   ├─ type: native     → QProcess 拉起 + 托盘托管  (预留,后续)
   ├─ type: embed      → XEmbed 窗口嵌入(X11 限定) (谨慎,受 Wayland 限制)
   └─ type: remote     → VDI / 云化推流           (可选)
```

无论哪种类型都继承合规能力,承载方式可插拔。**M1 只实现 web,但接口为 native 预留插槽**——以低成本保留扩展性。

### 8.4 关于"整体是一个应用"的体验诉求(重要取舍)
需求方倾向原生子应用也能有"整体是一个应用"的统一体验(如嵌入 Tab)。这与 §8.2 的 Wayland 约束存在冲突,须明确取舍:

- **若 V10 默认 X11**:可尝试 `type: embed`(XEmbed 窗口 reparent),实现原生应用嵌入 Tab 的融合体验,但仍有焦点/DPI/菜单弹窗错位等兼容问题,需真机验证。
- **若 V10 默认 Wayland**(新版趋势):窗口嵌入基本不可行,"统一体验"只能通过**体验层弥补**而非真嵌入:
  - 统一皮肤/标题栏/托盘/任务切换,让拉起的独立窗口"看起来是一家";
  - 门户提供统一的应用切换器(类似任务栏),弱化"另一个窗口"的割裂感;
  - 原生子应用同样从门户单点登录、继承权限与审计,做到"逻辑上是一个平台"。
- **务必先做的事**:在真机确认 V10 默认合成器(X11/Wayland),这是决定"能否真嵌入"的唯一前提(已列入 §7.2 待确认第 4 项)。

### 8.5 演进建议
1. **近期**:仅做 web 集成,但按"集成协议"抽象容器,为 native 留插槽。
2. **中期**:补 `type: native` 启动器式集成(`QProcess` 拉起 + 统一管理),配合 §8.4 体验层弥补统一感;投入产出比最高,回避 Wayland 嵌入难题。
3. **谨慎评估**:仅在确认 X11 且真机验证通过后,才投入 `type: embed` 真嵌入。
4. **不碰**:Windows C/S 老应用的"集成",一律走迁移。

---

_本文档为需求分析阶段产出,不含代码实现。技术栈与架构决策已定基线,细节随评审迭代。_
