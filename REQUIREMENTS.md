# 银行麒麟超级应用平台 —— 需求规格

> 文档版本:v1.0 ｜ 日期:2026-07-21 ｜ 状态:待评审
> 本版按评审意见重构:收敛为"范围 / 场景 / 功能需求 / 可验收非功能指标 / 明确不做什么";
> 技术决策移入 `docs/adr/`,专题设计移入 `docs/design/`,可行性验证见 `docs/M-1-可行性验证.md`。

---

## 0. 术语

| 术语 | 含义 |
|---|---|
| 门户 / 平台 | 本项目交付的麒麟原生外壳应用,统一入口与容器 |
| 子应用 | 被门户承接的应用。一期=B/S web 系统;二期=已适配麒麟的原生应用 |
| CAS | 行内单点登录服务;`TGC` 为其域下会话票据,`ST` 为一次性服务票据 |
| Profile | `QWebEngineProfile`,QtWebEngine 的会话/存储隔离单元 |
| 权限中心 | 行内 RBAC 权限主数据系统,门户对接方 |

---

## 1. 项目定位

在麒麟 V10 上交付一个**银行内网超级应用平台(原生外壳)**,作为统一入口承接行内 **B/S web 应用**,打通行内 **CAS 单点登录**与**权限中心**,提供子应用开发框架与统一基础能力,满足银行安全合规,交付接近原生的外壳体验。

本质是**信创迁移期的应用集成门户**,核心价值:统一入口容器、统一认证、安全合规底座。

---

## 2. 范围(经 2026-07-21 确认,建议需求方正式签字)

> 说明:以下一期/二期边界为本轮与需求方沟通确认的**范围决策**,非文档自行收敛。落项前建议需求方书面确认。

### 2.1 一期范围(In Scope)
- 麒麟 V10 双架构(ARM64 飞腾/鲲鹏 + x86_64)原生外壳,`.deb` 分发。
- 承接 **B/S web 子应用**:注册、加载、多标签导航、生命周期。
- **CAS 单点登录**与**单点登出**,web 子应用免登。
- 对接**权限中心**:门户侧菜单可见性/子应用启动权/JSBridge 能力授权。
- 基础能力:HTTP/WS/SSE、缓存、JSBridge、系统集成(托盘/通知/DPI/主题)。
- 安全合规底座:国密链路、审计日志、数据安全(具体指标见 §5)。
- 子应用开发框架:接入规范 + JS SDK + 脚手架。
- **架构预留**:子应用容器抽象为"集成协议",为二期原生集成留插槽(仅接口,不实现)。

### 2.2 二期范围(Out of Scope for 一期,架构预留)
- **原生 PC 应用集成**(已适配麒麟的原生应用):QProcess 拉起 + IPC 透票 + 统一管理。设计见 `docs/design/原生应用集成-IPC.md`,一期不实现。
- **资金领域专业组件(K 线等)**:作为平台之上的一个**业务子应用**后续交付,不属一期底座。

### 2.3 明确不做(Never / 走其他路线)
- 存量 **Windows C/S 应用**的"集成"——一律走迁移(重构为 B/S 或麒麟原生),不纳入本平台容器。
- 门户**不承担**子应用的业务级鉴权(见 §4 鉴权边界)。
- 门户**不自维护**权限主数据(以权限中心为准)。

---

## 3. 用户场景(一期)

1. **单点登录**:用户启动门户,完成一次 CAS 登录,进入工作台,看到有权限的子应用图标。
2. **免登进子应用**:点击某 web 子应用,门户新开标签加载,子应用经标准 CAS 跳转自动免登,直接进入业务界面。
3. **多应用并用**:同时打开多个子应用标签,切换流畅。
4. **单点登出**:用户在门户登出,所有子应用会话同步失效。
5. **权限差异**:不同角色用户登录,工作台可见/可启动的子应用不同。

---

## 4. 功能需求

### 4.1 麒麟适配(F1)
- **F1.1** x86_64 与 ARM64(飞腾/鲲鹏)双架构编译运行。
- **F1.2** `.deb` 安装包,支持行内内网分发/静默安装,含签名校验。
- **F1.3** 系统集成:窗口管理、托盘、桌面通知、DPI 缩放、主题跟随。
- **F1.4** 明确兼容的麒麟 V10 子版本与自带 Qt/QtWebEngine 版本基线(见 M-1 核定)。

### 4.2 认证(F2)—— 详见 `docs/design/SSO-CAS.md`
- **F2.1** 门户承载 CAS 登录:默认**内嵌 webview 打开 CAS 官方登录页**建立浏览器级 CAS 会话(TGC 在 CAS 域,不下发子应用)。
- **F2.2** web 子应用免登:各子应用走**标准 CAS 流程**(跳转 CAS → 凭 TGC 签发 ST → 子应用后端校验 ST 建会话)。门户不注入、不伪造 CAS Cookie。
- **F2.3** 单点登出:门户清理本地会话 + 调用 CAS `/logout` + 触发各子应用后端登出(CAS SLO)。
- **F2.4** 会话生命周期:TGC/ST 时效遵循 CAS 配置;门户处理会话过期与重新登录。

### 4.3 权限与鉴权边界(F3)—— 详见 `docs/design/权限与鉴权边界.md`
- **F3.1** 门户从**权限中心**加载当前用户 RBAC,粒度与权限中心一致。
- **F3.2** 门户**仅负责**:菜单可见性、子应用启动权、JSBridge 原生能力授权。
- **F3.3** **业务操作(转账/审批/敏感查询等)必须由子应用后端二次鉴权**。前端透传权限**不作为安全边界**。

### 4.4 web 容器(F4)—— 详见 `docs/adr/0003-profile隔离策略.md`
- **F4.1** 子应用注册/加载/卸载,多标签导航,生命周期管理。
- **F4.2** Profile 隔离策略按信任域划分(**非全局单一 Profile**,见 ADR-0003),兼顾 SSO 共享与安全隔离。
- **F4.3** 域名白名单与内容安全策略(CSP)。

### 4.5 JSBridge(F5)—— 详见 `docs/design/JSBridge.md`
- **F5.1** web ↔ 原生受控双向通信,能力按子应用授权暴露。
- **F5.2** 域名白名单 + 能力级鉴权 + 调用审计;敏感能力(如获取凭证)默认关闭。

### 4.6 基础能力 SDK(F6)
- **F6.1** 通信:HTTP(拦截器/重试/国密 TLS)、WebSocket(重连/心跳)、SSE。
  - 注:web 子应用自身已可用浏览器网络能力;本 SDK 主要服务**原生外壳侧**及需统一鉴权头/国密的场景,避免与浏览器能力重复造轮(回应评审"可能过度设计")。
- **F6.2** 缓存:内存 LRU + 磁盘持久化(敏感数据加密落盘或不落盘)+ TTL。
- **F6.3** 子应用开发框架:接入规范、JS SDK、脚手架、联调工具。

---

## 5. 非功能需求(可验收 —— 均须绑定硬件/镜像/数据/网络/百分位)

> 评审第 7 点:以下为**指标模板**,真实阈值须在 M-1 于真机基线测定后回填并锁定。性能与安全验证**从 M-1 开始**,不推迟到 M4。

| 编号 | 需求 | 可验收指标(模板,待 M-1 锁定) |
|---|---|---|
| N1 冷启动 | 门户冷启动 | 飞腾 D2000 / V10 指定镜像 / 冷启动 **P95 ≤ 3s**(x86 另定) |
| N2 子应用打开 | 点击到可交互 | 指定子应用 / 局域网 RTT ≤ 5ms / **P95 ≤ 2s**,无持续白屏 |
| N3 内存 | 门户常驻(空载) | 指定镜像 / 空载常驻 **≤ N MB**(N 于 M-1 实测定),含 3 子应用时 ≤ M MB |
| N4 切换 | 标签切换 | 指定硬件 / 切换渲染 **P95 ≤ 100ms** |
| N5 国密 | 通信国密 | 指定链路采用 SM2/SM3/SM4,国密 TLS 握手成功、可抓包验证(范围见待确认) |
| N6 等保 | 合规项 | 逐条映射行内等保定级 checklist,登录锁定/密码策略/超时锁屏等可测 |
| N7 审计 | 关键操作可追溯 | 登录/登出/换票/启动子应用/敏感能力调用 100% 落审计,字段对齐审计平台 |
| N8 稳定 | 长连接自愈 | 断网注入后 **≤ 30s** 自动恢复订阅 |

> 不可验收表述(如"媲美原生""明显优于 Electron""防逆向")一律替换为上述可测指标或映射到具体 checklist;"防逆向"改为"符合行内应用加固标准 X 条"。

---

## 6. 里程碑(性能/安全前置)

| 里程碑 | 目标 | 验收 |
|---|---|---|
| **M-1 可行性验证** | 真机(飞腾D2000/x86 + V10)验证 QtWebEngine 渲染、**标准 CAS 跳转**、一个真实子应用、`.deb` 安装、资源占用、原生应用拉起可行性 | 见 `docs/M-1-可行性验证.md`;锁定 §5 指标阈值 |
| **M0 骨架** | 双架构编译打包、门户外壳可启动 | 两架构均出 `.deb` 并启动 |
| **M1 命脉** | CAS 登录 + 内嵌一个真实 web 子应用免登(标准流程)+ 单点登出 | 登录一次,子应用免登,登出全失效 |
| **M2 容器+权限** | 多子应用/多标签 + 权限中心对接 + Profile 隔离策略落地 | 权限正确控制可见性/启动 |
| **M3 能力+安全** | HTTP/WS/SSE + 缓存 + 国密 + 审计 | N5/N7 可测通过 |
| **M4 框架+性能** | JS SDK + 脚手架 + 性能达标 | N1–N4/N8 达 M-1 锁定阈值 |

---

## 7. 原始需求覆盖对照(回应评审)

| 原始需求(CLAUDE.md) | 一期处置 | 说明 |
|---|---|---|
| 麒麟适配 | ✅ 一期 | V10 具体子版本 M-1 核定 |
| 企业认证与权限 | ✅ 一期 | 认证/鉴权模型已按评审修正(标准 CAS + 后端二次鉴权) |
| web 内嵌 | ✅ 一期 | 主形态 |
| 各类 PC 应用 | ⏭ 二期 | **经确认**列二期,架构预留插槽(§2.2) |
| HTTP/WS/SSE、缓存 | ✅ 一期(收敛) | 明确边界,不与浏览器能力重复(F6.1 注) |
| K 线专业组件 | ⏭ 二期 | **经确认**作为后续业务子应用(§2.2) |
| 原生体验 | ✅ 一期(量化) | 转为 §5 可验收指标,M-1 起验证 |

---

## 8. 待确认事项(阻塞项)

见各专题文档的"待确认"小节汇总:
1. CAS 版本/协议、门户与子应用信任域关系、是否强制 CA 证书登录 —— `docs/design/SSO-CAS.md`
2. 权限中心对接协议与数据模型 —— `docs/design/权限与鉴权边界.md`
3. 国密强制范围、指定国密库/密码机 —— 影响 N5、M3
4. V10 具体子版本、自带 Qt 版本、**默认合成器 X11/Wayland** —— M-1 核定
5. 首批接入子应用清单及各自登录/权限现状
6. 等保定级 checklist、审计平台对接方式、数据落地/防截屏要求
7. **范围正式签字**:一期只做 web、K 线与原生列二期(§2)

---

## 9. 文档地图

- `docs/adr/0001-技术栈-qt.md` —— Qt/QML 选型(含版本/授权约束)
- `docs/adr/0002-双架构.md`
- `docs/adr/0003-profile隔离策略.md` —— Profile 分域,解共享 vs 隔离冲突
- `docs/design/SSO-CAS.md` —— CAS 免登(已按评审修正核心模型)
- `docs/design/权限与鉴权边界.md` —— 门户 vs 子应用后端鉴权边界
- `docs/design/JSBridge.md`
- `docs/design/原生应用集成-IPC.md` —— 二期,预留
- `docs/M-1-可行性验证.md`

---

_本文档为需求规格,不含代码实现。范围决策待需求方书面确认后进入 M-1。_
