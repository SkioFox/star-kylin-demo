# 银行麒麟超级应用平台 —— 需求规格

> 文档版本:**v0.5 Draft** ｜ 日期:2026-07-21 ｜ 状态:草案(范围未签字、环境/性能基线未定,不作 v1.0)
> 本版按评审意见重构:收敛为"范围 / 场景 / 功能需求 / 可验收非功能指标 / 明确不做什么";
> 技术决策移入 `docs/adr/`,专题设计移入 `docs/design/`,可行性验证见 `docs/M-1-可行性验证.md`。
> **架构级待查阻塞项(未解,进 M0 前必须定)**:①门户身份闭环 ②Profile 隔离与全局 SSO 的取舍 —— 均取决于**行内既有免登机制调研**,见 `docs/design/SSO-CAS.md` 与 `docs/adr/0003`。

---

## 0. 术语

| 术语 | 含义 |
|---|---|
| 门户 / 平台 | 本项目交付的麒麟原生外壳应用,统一入口与容器 |
| 子应用 | 被门户承接的应用。一期=B/S web 系统;二期=已适配麒麟的原生应用 |
| CAS | 行内单点登录服务;`TGC` 为其域下会话票据,`ST` 为一次性服务票据 |
| Profile | `QWebEngineProfile`,QtWebEngine 的会话/存储隔离单元 |
| 权限中心 | 行内 RBAC 权限主数据系统,门户对接方 |

---

## 1. 项目定位

在麒麟 V10 上交付一个**银行内网超级应用平台(原生外壳)**,作为统一入口承接行内 **B/S web 应用**,打通行内 **CAS 单点登录**与**权限中心**,提供子应用开发框架与统一基础能力,满足银行安全合规,交付接近原生的外壳体验。

本质是**信创迁移期的应用集成门户**,核心价值:统一入口容器、统一认证、安全合规底座。

---

## 2. 范围(本轮沟通倾向,**未签字**)

> 状态订正(评审第 8 点):以下一期/二期边界是本轮沟通中的**倾向性意见**,**尚未获需求方书面确认,也未在决策记录中固化**。签字前不视为已定范围。范围一经确认,记入 `docs/adr/0004-范围决策.md`;原始需求文件(CLAUDE.md)保持不变,变化只进决策记录以保留追溯。

### 2.1 一期范围(In Scope)
- 麒麟 V10 双架构(ARM64 飞腾/鲲鹏 + x86_64)原生外壳,`.deb` 分发。
- 承接 **B/S web 子应用**:注册、加载、多标签导航、生命周期。
- **CAS 单点登录**与**单点登出**,web 子应用免登。
- 对接**权限中心**:门户侧菜单可见性/子应用启动权/JSBridge 能力授权。
- 基础能力:HTTP/WS/SSE、缓存、JSBridge、系统集成(托盘/通知/DPI/主题)。
- 安全合规底座:国密链路、审计日志、数据安全(具体指标见 §5)。
- 子应用开发框架:接入规范 + JS SDK + 脚手架。
- **架构预留**:子应用容器抽象为"集成协议",为二期原生集成留插槽(仅接口,不实现)。

### 2.2 二期范围(Out of Scope for 一期,架构预留)
- **原生 PC 应用集成**(已适配麒麟的原生应用):QProcess 拉起 + IPC 透票 + 统一管理。设计见 `docs/design/原生应用集成-IPC.md`,一期不实现。
- **资金领域专业组件(K 线等)**:作为平台之上的一个**业务子应用**后续交付,不属一期底座。

### 2.3 明确不做(Never / 走其他路线)
- 存量 **Windows C/S 应用**的"集成"——一律走迁移(重构为 B/S 或麒麟原生),不纳入本平台容器。
- 门户**不承担**子应用的业务级鉴权(见 §4 鉴权边界)。
- 门户**不自维护**权限主数据(以权限中心为准)。

---

## 3. 用户场景(一期)

1. **单点登录**:用户启动门户,完成一次登录,门户**获知当前用户身份**(见 F2.0,机制待调研),据此查询权限中心,进入工作台看到有权限的子应用。
2. **免登进子应用**:点击某 web 子应用,门户新开标签加载,子应用经行内既有免登机制自动免登,直接进入业务界面。
3. **多应用并用**:同时打开多个子应用标签,切换流畅。
4. **登出**(能力分层,见 F2.3):门户可保证**本机会话清理**与 **CAS TGC 失效**;子应用**服务端 Session 失效仅对支持 SLO 的应用保证**。不承诺"所有子应用服务端会话必然失效"。
5. **权限差异**:不同角色用户登录,工作台可见/可启动的子应用不同。

---

## 4. 功能需求

### 4.1 麒麟适配(F1)
- **F1.1** x86_64 与 ARM64(飞腾/鲲鹏)双架构编译运行。
- **F1.2** `.deb` 安装包,支持行内内网分发/静默安装,含签名校验。
- **F1.3** 系统集成:窗口管理、托盘、桌面通知、DPI 缩放、主题跟随。
- **F1.4** 明确兼容的麒麟 V10 子版本与自带 Qt/QtWebEngine 版本基线(见 M-1 核定)。

### 4.2 认证(F2)—— 详见 `docs/design/SSO-CAS.md`
- **F2.0 门户身份闭环(架构级待查阻塞)**:门户登录后必须能**安全获知"当前用户是谁"**,才能查询权限中心。仅拿到浏览器里的 HttpOnly TGC,原生 Qt 侧无法安全得到身份。方案取决于**行内既有免登机制调研**(见 SSO 专题),候选:门户 BFF 注册为 CAS Service 后端校验 ST 建门户会话(推荐)/ 经 CAS 批准的本地回环收 ST 校验 / 行内若为域集成或客户端 agent 则另议。**未定,进 M0 前必须解决。**
- **F2.1** 门户承载登录:默认假设为**内嵌 webview 打开行内登录页**建立浏览器级会话;实际以调研到的行内机制为准。
- **F2.2** web 子应用免登:优先复用**行内既有免登机制**(如标准 CAS:跳转→凭 TGC 签发 ST→子应用后端校验 ST 建会话)。门户不注入、不伪造凭证。
- **F2.3** 登出(能力分层,不超出平台控制力):① 本机会话清理(平台保证)② CAS `/logout` 使 TGC 失效(CAS 保证)③ 子应用服务端 Session 失效(**仅对支持 SLO 的子应用保证**)。
- **F2.4** 会话生命周期:票据时效遵循行内配置;门户处理会话过期与重新登录。

### 4.3 权限与鉴权边界(F3)—— 详见 `docs/design/权限与鉴权边界.md`
- **F3.1** 门户从**权限中心**加载当前用户 RBAC,粒度与权限中心一致。
- **F3.2** 门户**仅负责**:菜单可见性、子应用启动权、JSBridge 原生能力授权。
- **F3.3** **业务操作(转账/审批/敏感查询等)必须由子应用后端二次鉴权**。前端透传权限**不作为安全边界**。

### 4.4 web 容器与应用治理(F4)—— 详见 `docs/adr/0003` 与 `docs/design/应用清单与治理.md`
- **F4.1** 子应用注册/加载/卸载,多标签导航,生命周期管理。
- **F4.2** Profile 策略:**默认统一 Profile 以保证全局 SSO**;仅**明确接受再次认证**的高风险应用用独立 Profile(见 ADR-0003)。
- **F4.3** 域名白名单与内容安全策略(CSP)。
- **F4.4 应用清单契约**:一期至少需要一份**签名的应用清单**(字段:appId、入口 URL、允许来源、Profile 分组、CAS Service、Bridge 能力、图标、版本、打开方式),含**签名/更新/回滚**规则;可复用行内配置分发,不强制自建管理后台。
- **F4.5 运行治理责任**:明确 QtWebEngine/Chromium **CVE 升级、SBOM、安装包更新**的责任人与周期。

### 4.5 JSBridge(F5)—— 详见 `docs/design/JSBridge.md`
- **F5.1** web ↔ 原生受控双向通信,能力按子应用授权暴露。
- **F5.2** 绑定 **QWebEnginePage + 精确 scheme/host/port**(非仅按 Profile);页面跳转即撤销 Bridge;限制 iframe/弹窗/重定向页面访问;每次调用重校验 Origin。
- **F5.3** 参数类型/长度/文件路径规范化 + 超时;高风险能力(文件/打印/设备)需用户确认;审计参数脱敏。
- **F5.4** 敏感能力(如获取凭证)默认关闭;**登录页/被劫持的跳转页绝不获得 Bridge**。

### 4.6 基础能力 SDK(F6)
- **F6.1** 通信:HTTP(拦截器/重试/国密 TLS)、WebSocket(重连/心跳)、SSE。
  - 注:web 子应用自身已可用浏览器网络能力;本 SDK 主要服务**原生外壳侧**及需统一鉴权头/国密的场景,避免与浏览器能力重复造轮(回应评审"可能过度设计")。
- **F6.2** 缓存:内存 LRU + 磁盘持久化(敏感数据加密落盘或不落盘)+ TTL。
- **F6.3** 子应用开发框架:接入规范、JS SDK、脚手架、联调工具。

---

## 5. 非功能需求(可验收 —— 均须绑定硬件/镜像/数据/网络/百分位)

> 评审第 7 点:以下为**指标模板**。阈值**不能仅按"当前实现跑出多少"倒推**,须由 **M-1 实测结果 + 业务可接受值共同确定**。所有性能项须约定:**采样次数、冷/热缓存、计时起止点、P95 计算方式**(见 M-1 §测量方法)。性能与安全验证**从 M-1 开始**,不推迟到 M4。

| 编号 | 需求 | 可验收指标(模板,待 M-1 锁定) |
|---|---|---|
| N1 冷启动 | 门户冷启动 | 飞腾 D2000 / V10 指定镜像 / 冷缓存 / 计时=进程启动→工作台可交互 / N≥20 次 / **P95 ≤ 3s**(x86 另定) |
| N2 子应用打开 | 点击到可交互 | 指定子应用 / 局域网 RTT ≤ 5ms / 计时=点击→页面可交互 / **P95 ≤ 2s**,无持续白屏 |
| N3 内存 | 门户常驻 | 指定镜像 / 空载 **≤ N MB**、含 3 子应用 ≤ M MB(N/M 由 M-1 实测+业务可接受值定) |
| N4 切换 | 标签切换 | 指定硬件 / 计时=切换触发→首帧 / **P95 ≤ 100ms** |
| N5 国密 | 通信国密(**分链路**) | 见下"国密分链路",每条链路分别验证握手成功、抓包确认套件 |
| N6 等保 | 合规项 | 逐条映射行内等保定级 checklist,登录锁定/密码策略/超时锁屏等可测 |
| N7 审计 | 关键操作可追溯 | 登录/登出/换票/启动子应用/敏感能力调用 100% 落审计,字段对齐审计平台 |
| N8 稳定 | 长连接自愈 | 断网注入后 **≤ 30s** 自动恢复订阅 |

**N5 国密分链路(评审第 5 点)—— 详见 `docs/design/国密链路.md`**:国密须**分三条链路**各自明确(协议版本/密码套件/证书体系/密码模块/是否允许网关终止国密),原生 SDK 支持国密**不代表** Chromium 网络栈支持:
1. QtWebEngine/Chromium 加载子应用的链路;2. Qt 原生网络 SDK 链路;3. CAS/权限中心/审计平台通信链路。
> **此答案须在 M-1 开始前取得**,不再以"若已知范围"处理。

> 不可验收表述(如"媲美原生""明显优于 Electron""防逆向")一律替换为可测指标或 checklist;"防逆向"改为"符合行内应用加固标准 X 条"。

---

## 6. 里程碑(性能/安全前置)

| 里程碑 | 目标 | 验收 |
|---|---|---|
| **M-1 可行性验证** | 真机(飞腾D2000/x86 + V10)验证 QtWebEngine 渲染、行内既有免登机制、**门户身份闭环**、同/跨 Profile 免登两种情况、一个真实子应用、`.deb` 安装、资源占用、原生应用拉起 | 见 `docs/M-1-可行性验证.md`;锁定 §5 指标 + 解 F2.0/ADR-0003 阻塞 |
| **M0 骨架** | 双架构编译打包、门户外壳可启动(**须先解 F2.0 身份闭环 + Profile 取舍**) | 两架构均出 `.deb` 并启动 |
| **M1 命脉** | 登录 + 门户获知身份 + 内嵌一个真实 web 子应用免登 + 分层登出 | 登录一次→子应用免登;登出达 F2.3 分层保证 |
| **M2 容器+权限** | 多子应用/多标签 + 权限中心对接 + 应用清单 + Profile 策略落地 | 权限正确控制可见性/启动 |
| **M3 能力+安全** | HTTP/WS/SSE + 缓存 + 国密 + 审计 | N5/N7 可测通过 |
| **M4 框架+性能** | JS SDK + 脚手架 + 性能达标 | N1–N4/N8 达 M-1 锁定阈值 |

---

## 7. 原始需求覆盖对照(回应评审)

| 原始需求(CLAUDE.md) | 一期处置 | 说明 |
|---|---|---|
| 麒麟适配 | ✅ 一期 | V10 具体子版本 M-1 核定 |
| 企业认证与权限 | ✅ 一期 | 认证/鉴权模型已按评审修正(标准 CAS + 后端二次鉴权) |
| web 内嵌 | ✅ 一期 | 主形态 |
| 各类 PC 应用 | ⏭ 二期(**倾向,待签字**) | 列二期,架构预留插槽(§2.2) |
| HTTP/WS/SSE、缓存 | ✅ 一期(收敛) | 明确边界,不与浏览器能力重复(F6.1 注) |
| K 线专业组件 | ⏭ 二期(**倾向,待签字**) | 作为后续业务子应用(§2.2) |
| 原生体验 | ✅ 一期(量化) | 转为 §5 可验收指标,M-1 起验证 |

---

## 8. 待确认事项

### 8.1 架构级阻塞(进 M0 前必须定)
1. **行内既有免登机制调研**(最高优先):行内 Windows 应用"登一次、开其他系统免登"是靠什么?——域集成(Kerberos/AD)/ 客户端 agent 持票 / 浏览器 CAS?**答案直接决定 F2.0 门户身份闭环与 ADR-0003 Profile 取舍。**
2. **门户身份闭环方案**(F2.0):BFF 注册 CAS Service 后端校验 ST / 本地回环收 ST / 域集成 —— 依赖第 1 项。
3. **Profile 与全局 SSO 取舍**(ADR-0003):默认统一 Profile;独立 Profile 仅用于可接受再认证的应用。M-1 须实测同/跨 Profile 两种。

### 8.2 设计定稿阻塞
4. 国密**分三链路**的协议/套件/证书/密码模块/是否网关终止 —— `docs/design/国密链路.md`,**M-1 前取得**。
5. 权限中心对接协议与数据模型 —— `docs/design/权限与鉴权边界.md`。
6. 应用清单格式/签名/更新/回滚 + CVE/SBOM 责任 —— `docs/design/应用清单与治理.md`。
7. 各子应用是否支持 CAS SLO(决定 F2.3 第③层)。

### 8.3 基线与流程
8. V10 具体子版本、自带 Qt 版本、**默认合成器 X11/Wayland** —— M-1 核定。
9. 首批接入子应用清单及各自登录/权限现状。
10. 等保定级 checklist、审计平台对接方式、数据落地/防截屏要求。
11. **范围正式签字**:一期只做 web、K 线与原生列二期(§2 / ADR-0004)。

---

## 9. 文档地图

- `docs/adr/0001-技术栈-qt.md` —— Qt/QML 选型(含版本/授权约束)
- `docs/adr/0002-双架构.md`
- `docs/adr/0003-profile隔离策略.md` —— 默认统一 Profile 保 SSO,独立 Profile 需接受再认证
- `docs/adr/0004-范围决策.md` —— 一期/二期范围决策记录(待签字)
- `docs/design/SSO-CAS.md` —— 免登与门户身份闭环(含待查阻塞)
- `docs/design/权限与鉴权边界.md` —— 门户 vs 子应用后端鉴权边界
- `docs/design/JSBridge.md` —— 落地级安全边界
- `docs/design/国密链路.md` —— 分三链路国密要求
- `docs/design/应用清单与治理.md` —— 签名清单/更新回滚/CVE/SBOM
- `docs/design/原生应用集成-IPC.md` —— 二期,预留
- `docs/M-1-可行性验证.md`
- `docs/开发与构建环境.md` —— Intel Mac 外网开发 / 内网验证 / 摆渡与离线依赖
- `docs/行内对接确认清单.md` —— 全部待确认项按 P0–P3 分级(对接用)

---

_本文档为需求规格草案(v0.5),不含代码实现。范围签字 + M-1 环境/性能基线回填后方升 v1.0 并进入 M0。_
