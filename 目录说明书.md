# star-kylin-demo 项目目录说明书

> 版本 v0.1 ｜ 2026-07-24
> 项目:银行麒麟超级应用平台 —— 信创迁移 + 应用集成门户

---

## 速览:项目有什么、为什么这样分

```
star-kylin-demo/
├── 📋 根文档(4 份)                    ← 项目入口,回答"这是什么项目、范围多大"
├── 📁 docs/                           ← 生产级需求分析、架构决策、专题设计
│   ├── adr/                           ← 技术决策记录(为什么选 Qt、为什么双架构…)
│   └── design/                        ← 专题设计(CAS、权限、JSBridge、国密…)
└── 💻 mvp-demo/                        ← MVP Demo 独立工程(需求/方案/任务/源码/产物)
    └── 📖 详见 mvp-demo/目录说明书.md
```

**核心区分:**
- `docs/` — 面向**生产级**的分析与设计(CAS 真实对接、国密、权限中心、原生 IPC…),mvp-demo 完工后继续用
- `mvp-demo/` — 面向 **Demo 演示**的独立闭环(登录 mock、K 线 mock、单机 `.deb`),有自己的需求、方案和源码

---

## 一、根目录文件(4 份)

| 文件 | 是什么 | 怎么看 / 什么时候看 |
|---|---|---|
| `CLAUDE.md` | **原始需求概念**:一句话定义 + 4 条核心要求(麒麟适配 / 应用底座 / 基础能力 / 性能)。项目的出发点 | 新成员**最先读这份**,理解项目存在的理由。这份文件不再修改,变化只记入 ADR |
| `REQUIREMENTS.md` | **生产级需求规格 v0.5 Draft**:范围、用户场景、功能需求(F1–F6)、可验收非功能指标(N1–N8)、里程碑(M-1→M4)、原始需求覆盖对照 | 理解**完整产品目标**(不只是 Demo)。范围未签字、环境基线未定前不视为正式需求 |
| `Agents.md` | **引用 `@CLAUDE.md`**,把 AI 编码助手的上下文指向原始需求 | 不用手动看,工具自动加载 |
| `Talk-History.md` | **需求对话历史**:记录与需求方沟通的关键结论、决策背景、评审意见反馈 | 追溯某个决策的来龙去脉(如"为什么 K 线列二期") |
| `.gitignore` | Git 忽略规则:排除 IDE 配置、日志、构建产物、环境文件 | 不直接编辑 |

---

## 二、docs/ — 生产级分析与设计

这些文档面向**最终产品**(非 Demo 阶段),包含 Demo 不涉及的真 CAS、国密、权限中心、原生 IPC 等。

### 📁 docs/adr/ — 架构决策记录(4 篇)

| 文件 | 决策 | 状态 |
|---|---|---|
| `0001-技术栈-qt.md` | 外壳选 Qt/QML(C++),web 容器选 QtWebEngine | 已接受 |
| `0002-双架构.md` | 支持 ARM64(飞腾/鲲鹏)+ x86_64 | 已接受 |
| `0003-profile隔离策略.md` | **默认统一 Profile 保证全局 SSO**;独立 Profile 仅限可接受再认证的高风险应用 | 已接受 |
| `0004-范围决策.md` | 一期只做 web、K 线与原生列二期 | **待需求方签字** |

### 📁 docs/design/ — 专题设计(6 篇)

| 文件 | 解决什么问题 | Demo 用不用 |
|---|---|---|
| `SSO-CAS.md` | 门户身份闭环、CAS 免登机制、SLO 能力分层。含**架构级待查阻塞**(需调研行内既有免登机制) | ❌ Demo 用 Mock 登录替身 |
| `权限与鉴权边界.md` | 门户只管菜单可见性/启动权/JSBridge 授权;**业务操作一律子应用后端二次鉴权** | ❌ Demo 无真权限中心 |
| `JSBridge.md` | web ↔ 原生受控通信的落地级安全边界(Origin 绑定/跳转撤销/审计脱敏) | ❌ Demo 不实现 JSBridge |
| `国密链路.md` | 拆 L1(Chromium)/L2(原生SDK)/L3(CAS等)三条链路各自定国密要求 | ❌ Demo 不涉及国密 |
| `应用清单与治理.md` | 子应用注册契约:签名清单字段、CVE/SBOM/更新责任 | ⚠️ Demo 的 manifest.json 是其最小子集 |
| `原生应用集成-IPC.md` | **二期**——原生应用 IPC 透票与窗口集成设计 | ⚠️ Demo 仅实现启动器式(QProcess 拉起,不传票据) |

### docs/ 其他文件(3 份)

| 文件 | 是什么 | 什么时候用 |
|---|---|---|
| `M-1-可行性验证.md` | 真机验证清单:13 项验证(V1–V13)+ 测量方法 + 指标锁定流程。**M-1 排在 M0 之前** | T0A 拿到验收机后照着执行 |
| `开发与构建环境.md` | Intel Mac 外网开发 / 麒麟内网验证 / 摆渡与离线依赖的策略 | 搭建开发环境和构建通道时参考 |
| `行内对接确认清单.md` | 全部待确认项按 P0–P3 分级:阻塞开发模式 / 阻塞架构 / 阻塞设计定稿 / 基线。每项带对接方 | **去行内对接时拿着这份**,逐项确认 |

---

## 三、mvp-demo/ — MVP Demo 独立工程

详见 `mvp-demo/目录说明书.md`。这里只给顶层轮廓:

```
mvp-demo/
├── 📋 文档(7 份)              需求基线 → 技术方案 → 任务拆解 → 构建指南
├── 🎨 ui-prototype/           HTML/CSS 浏览器高保真原型(UI 设计基线,非正式程序)
└── 💻 app/
    ├── ✍️ src/ qml/ resources/ tests/  ← 你写的 Qt/C++ 源码
    ├── 🔧 tools/                   ← build-deb.sh、Mock 数据生成、图标提取
    ├── 📦 packaging/               ← .deb 的 .desktop、图标、Dockerfile
    ├── 🔨 build-*/                 ← CMake 构建中间产物(自动生成,不进 git)
    ├── 📂 staging-*/               ← cmake --install 安装树(自动生成)
    ├── 🚀 dist/                    ← 最终的 .deb 安装包(交付物)
    └── 🧪 output/ package-verify/  ← 冒烟测试/打包验证(自动生成)
```

---

## 四、两套文档的关系

| 维度 | docs/(生产级) | mvp-demo/(Demo) |
|---|---|---|
| 阶段 | MVP 之后继续用 | 当前开发与验收用 |
| 认证 | 真 CAS / SSO / SLO | Mock 登录(假账号) |
| 权限 | 对接行内权限中心 | Manifest 内置角色 |
| 安全 | 国密 / 审计 / 等保 | 6 条安全红线(S1–S6) |
| K 线 | 真行情 + 原生自绘(二期) | 本地 ECharts + Mock 数据 |
| 打包 | 生产签名/灰度/回滚 | CPack DEB + SHA-256 |
| 冲突时 | 不阻塞 Demo | Demo 有独立需求文档 |

**原则:**
- mvp-demo 不依赖生产级文档,生产级文档也不约束 Demo 的 Mock 决策。
- Demo 完成后的经验回填到生产级设计(如 Profile 策略、UrlPolicy 模式)。
- CLAUDE.md 是唯一不变的原始需求锚点,变化只进 ADR。

---

## 五、常见操作速查

| 我想... | 怎么做 |
|---|---|
| 理解项目是什么 | 读 `CLAUDE.md`(1 分钟)→ `REQUIREMENTS.md` §1(5 分钟) |
| 理解完整产品目标 | 读 `REQUIREMENTS.md` + `docs/adr/*` + `docs/design/*` |
| 理解 MVP Demo 做什么 | 读 `mvp-demo/MVP-demo需求.md` |
| 理解 MVP 怎么实现 | 读 `mvp-demo/MVP技术方案.md` |
| 开始写 MVP 代码 | 读 `mvp-demo/MVP任务拆解.md` → 读 `mvp-demo/目录说明书.md` → 看 `mvp-demo/app/` |
| 出 .deb 包 | `cd mvp-demo/app && ./tools/build-deb.sh` |
| 去行内对接带着什么 | `docs/行内对接确认清单.md`(P0–P3 分级) |
| 追溯某决策为什么这么做 | `docs/adr/`(架构决策) 或 `Talk-History.md`(讨论历史) |
| 看 UI 长什么样 | `mvp-demo/UI原型与功能规格.md` + 启动 `mvp-demo/ui-prototype/server.mjs` |
| 分清哪些文件我该编辑/哪些不该碰 | 源码 = 编辑;`build-*/staging-*/dist/` = 自动生成,不碰 |

---

## 六、规则

1. **CLAUDE.md 是原始需求锚点**,不再修改;需求变化记入 `docs/adr/` 和 `Talk-History.md`。
2. **REQUIREMENTS.md 是生产级需求草案(v0.5)**,范围未签字、环境基线未定前不作正式基线。
3. **mvp-demo/ 是独立闭环**,有自己的需求→方案→任务→源码→产物,不依赖生产级文档。
4. **所有 `build-*/staging-*/dist/` 产物不进 git**,在 `.gitignore` 排除或构建前清空。
5. **docs/design/ 的专题设计面向生产**,Demo 阶段可参考其中的安全边界(如 UrlPolicy、Profile 隔离),但不实现真 CAS/国密/审计。
